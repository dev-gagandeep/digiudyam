import "server-only";import type { ProviderAdapter } from "../core";import { ProviderNotConfiguredError } from "../core";
export const googleAdapter:ProviderAdapter={id:"google",capabilities:{analytics:true,search:true,localPresence:true,ads:true,leads:false,appointments:false,conversations:false,automations:false},async validateConfiguration(){return Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET)},async getAuthorizationUrl(){throw new ProviderNotConfiguredError("google")},async exchangeAuthorizationCode(){throw new ProviderNotConfiguredError("google")},async sync(){throw new ProviderNotConfiguredError("google")}};
