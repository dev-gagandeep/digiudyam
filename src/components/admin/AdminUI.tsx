import Link from "next/link";import { ArrowRight,Warning } from "@phosphor-icons/react/dist/ssr";import type { Health } from "@/lib/admin/types";
export function AdminHeading({label,title,copy,action}:{label:string;title:string;copy?:string;action?:React.ReactNode}){return <div className="admin-heading"><div><span>{label}</span><h1>{title}</h1>{copy&&<p>{copy}</p>}</div>{action}</div>}
export function HealthBadge({health}:{health:Health}){return <span className={`health-badge health-${health.toLowerCase().replace(" ","-")}`}><i/>{health}</span>}
export function OpsAttention({items}:{items:{severity:string;title:string;copy:string;href:string}[]}){return <section className="ops-attention"><header><div><Warning weight="fill"/>NEEDS ATTENTION</div><b>{items.length} OPEN SIGNALS</b></header>{items.map((x,i)=><article key={x.title}><span className={`signal-${x.severity}`}>0{i+1} / {x.severity}</span><div><h3>{x.title}</h3><p>{x.copy}</p></div><Link href={x.href}>Review <ArrowRight/></Link></article>)}</section>}
export function AdminEmpty({title,copy}:{title:string;copy:string}){return <div className="admin-empty"><span>QUEUE CLEAR</span><h3>{title}</h3><p>{copy}</p></div>}
