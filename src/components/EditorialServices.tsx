"use client";
import { useState } from "react"; import { ArrowRight, Browser, MapPin, Megaphone, Robot } from "@phosphor-icons/react";
const services=[{n:"01",key:"BUILD",title:"Turn your online presence into a working storefront.",items:"Website / Landing Pages / E-commerce",icon:Browser,stat:"Ready to convert",signal:"Fast · Clear · Credible"},{n:"02",key:"GET FOUND",title:"Show up at the exact moment local customers search.",items:"SEO / Local SEO / Google Maps / Listings / Authority",icon:MapPin,stat:"Visibility mapped",signal:"Search · Maps · Trust"},{n:"03",key:"GET CUSTOMERS",title:"Create demand and turn attention into conversations.",items:"Ads / Reviews / Content / Lead Generation",icon:Megaphone,stat:"Enquiries tracked",signal:"Reach · Proof · Leads"},{n:"04",key:"AUTOMATE",title:"Make sure every new opportunity gets a proper follow-up.",items:"CRM / WhatsApp / AI / Follow-up Automation",icon:Robot,stat:"Follow-up active",signal:"Capture · Respond · Grow"}];
export function EditorialServices(){const [active,setActive]=useState(0);const current=services[active];const Icon=current.icon;return <div className="service-index-wrap"><div className="service-index" role="list">{services.map((s,i)=><button role="listitem" key={s.key} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)} onClick={()=>setActive(i)} className={active===i?"active":""} aria-pressed={active===i}><span>{s.n}</span><div><b>{s.key}</b><small>{s.items}</small></div><ArrowRight/></button>)}</div><aside className="service-display" aria-live="polite"><div className="display-grid"/><header><span>CAPABILITY / {current.n}</span><i>ACTIVE</i></header><Icon weight="thin"/><h3>{current.title}</h3><div className="display-status"><span><i/>{current.stat}</span><b>{current.signal}</b></div></aside></div>}
