import { ArrowRight, FilePdf } from "@phosphor-icons/react/dist/ssr"; import { PortalHeading } from "@/components/portal/PortalUI"; import { getPortalData } from "@/lib/portal/provider";
export const metadata={title:"Reports"};export default async function Reports(){const d=await getPortalData();return <div className="portal-page"><PortalHeading eyebrow="SYSTEM / REPORTS" title="A clear monthly view of progress and next steps." copy="Executive summaries across website, search, Google, leads, reviews, advertising and automation."/><div className="report-list">{d.reports.map((r,i)=><article key={r.id}><span>REPORT / {String(i+1).padStart(2,"0")}</span><div><h2>{r.title}</h2><p>{r.period}</p><small>{r.summary}</small></div><b className={`report-${r.status.toLowerCase()}`}>{r.status}</b><button disabled={r.status!=="Ready"} title="PDF downloads require production report storage"><FilePdf/> PDF unavailable</button><ArrowRight/></article>)}</div><section className="report-anatomy"><span>REPORT ARCHITECTURE</span><div>{["Executive Summary","Website","SEO","Google","Leads","Reviews","Advertising","Automation","Work Completed","Next Month Plan"].map((x,i)=><p key={x}><b>0{i+1}</b>{x}</p>)}</div></section></div>}
